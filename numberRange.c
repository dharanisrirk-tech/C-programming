#include <stdio.h>
int main() {
    int num = 15;
    if (num >= 10 && num <= 20) {
        printf("Number is within range");
    } else {
        printf("Number is outside range");
    }
    return 0;
}
