#include <stdio.h>
int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    a *= b;  
    printf("%d", a);
    int c, d;
    scanf("%d %d", &c, &d);
    c *= d;  
    printf("%d", c);
    int e, f;
    scanf("%d %d", &e, &f);
    e *= f;  
    printf("%d", e);
    return 0;
}
